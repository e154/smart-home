language: go

go:
  - 1.7.x

cache:
  directories:
    - node_modules # NPM packages

before_install:
  - npm install -g bower
  - go env && pwd

install:
  - go get github.com/FiloSottile/gvt
  - ./travis/make_all.sh

notifications:
  email: false