language: go

go:
  - 1.7.x

cache:
  directories:
    - node_modules # NPM packages

install:

before_install:
  - npm install -g bower
  - go env && pwd

script:
  - go get github.com/FiloSottile/gvt
  - ./travis/make_all.sh

notifications:
  email: false