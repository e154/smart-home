language: go

go:
  - 1.7.x

cache:
  directories:
    - node_modules # NPM packages

env:
  global:
    - GO15VENDOREXPERIMENT='0'

before_install:
  - npm install -g bower

script:
  - go get github.com/FiloSottile/gvt
  - ./travis/make_all.sh

notifications:
  email: false