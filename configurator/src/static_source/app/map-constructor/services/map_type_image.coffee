angular
.module('angular-map')
.factory 'mapTypeImage', ['$rootScope', '$compile', 'Message', 'Notify'
  ($rootScope, $compile, Message, Notify) ->
    class mapTypeImage

      scope: null

      constructor: (@scope)->

    mapTypeImage
]