%img.unselectable( ng-src="{{state.image.url}}" ng-repeat="state in element.prototype.states" ng-if="state.device_state.system_name == 'DEFAULT'" ng-style="{{state.style}}" ng-class="{'draggable-entity' : element.status !='frozen'}")
