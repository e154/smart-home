export interface ItemPayloadColorPicker {
    color?: string;
    attribute?: string;
    action?: string;
    entityId?: string;
    entity?: { id: string };
    tags?: string[],
    areaId?: number,
}
