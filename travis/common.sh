#!/usr/bin/env bash

BASEDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
CONFIGURATOR="${BASEDIR}/../configurator"
NODE="${BASEDIR}/../node"
SERVER="${BASEDIR}/../server"
TMP_DIR="${BASEDIR}/../tmp"
